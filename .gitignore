# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
env/

# pytest / allure
.pytest_cache/
.coverage
htmlcov/
reports/allure-results/
reports/allure-report/
allure-results/
allure-report/
*.xml

# IDE / 系统
.idea/
.vscode/
.DS_Store
Thumbs.db

# 抓包证据（体积大，单独存本地即可，不推仓库）
*.chls
*.saz
*.har

# 临时文件
*.log
*.tmp

# 依赖（项目根误装，不推）
node_modules/

# 抓包/运行时产物（每次运行重新生成，不推仓库；保留 contract_baseline.json 作为契约基准）
mitm_assertions.json
mitm_mock_events.json
ws_messages.json
perf_report.json
chaos_report.json
quality_metrics.json
coverage.xml
perf_stats*.csv
server.log

# 报告（allure 重新生成）
reports/

# 数据库（运行时生成，每次从播种状态重建）
*.db
*.sqlite