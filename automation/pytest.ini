[pytest]
testpaths = .
python_files = test_*.py
python_classes = Test*
python_functions = test_*
# 默认不加 allure，避免本地无 allure 环境时跑挂；要生成 allure 报告就设环境变量 ALLURE_ON=1
addopts = -vs --tb=short -ra
filterwarnings =
    ignore::DeprecationWarning